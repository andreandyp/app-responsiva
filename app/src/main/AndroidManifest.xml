<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andreandyp.responsiveapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ResponsiveApp">
        <activity
            android:name=".ChooserActivity"
            android:exported="true"
            android:theme="@style/Theme.ResponsiveApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BeerListActivity"
            android:label="@string/title_beer_list"
            android:theme="@style/Theme.ResponsiveApp" />
        <activity
            android:name=".compose1.BeerComposeFragmentsActivity"
            android:exported="false"
            android:theme="@style/Theme.ResponsiveApp" />
    </application>

</manifest>
